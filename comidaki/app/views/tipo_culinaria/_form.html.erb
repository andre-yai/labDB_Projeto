<%= form_for(@tipo_culinarium) do |f| %>
  <% if @tipo_culinarium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_culinarium.errors.count, "error") %> prohibited this tipo_culinarium from being saved:</h2>

      <ul>
      <% @tipo_culinarium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
