<p id="notice"><%= notice %></p>

<h1>Listing Cliente Tem Enderecos</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cliente_tem_enderecos.each do |cliente_tem_endereco| %>
      <tr>
        <td><%= link_to 'Show', cliente_tem_endereco %></td>
        <td><%= link_to 'Edit', edit_cliente_tem_endereco_path(cliente_tem_endereco) %></td>
        <td><%= link_to 'Destroy', cliente_tem_endereco, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cliente tem endereco', new_cliente_tem_endereco_path %>
